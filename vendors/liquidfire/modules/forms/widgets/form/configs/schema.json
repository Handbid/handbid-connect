{
    "properties": {

        "formSchema": {
            "type":    "object",
            "options": {
                "label":       "Schema",
                "description": "The schema you want to render"
            }
        },

        "formValues": {
            "type":    "object",
            "options": {
                "label":       "Values",
                "description": "The values that will be used to populate schema."
            }
        },

        "formOptions": {
            "type":    "object",
            "options": {
                "label":       "Form options",
                "description": "Options to pass through to the form"
            }
        },

        "viewPaths": {
            "type":    "string",
            "options": {
                "label":       "View Paths",
                "many":        true,
                "description": "I will pass these paths to liquidfire:Forms/templates/Resolver"
            }
        },

        "enctype": {
            "type":    "string",
            "options": {
                "label": "Enctype",
                "value": "application/x-www-form-urlencoded"
            }
        },

        "method": {
            "type":    "select",
            "options": {
                "label":   "Method",
                "default": "POST",
                "choices": {
                    "POST": "POST",
                    "GET":  "GET"

                }

            }
        },

        "maxCols": {
            "type":    "number",
            "options": {
                "label":       "Maximum columns",
                "description": "Css libraries (such as twitter bootstrap) have a grid system. You can specify the most amount of columns each row can have here. This will add a class to each element wrapper using the colClass template.",
                "value":       12
            }
        },

        "colClass": {
            "type":    "string",
            "options": {
                "label":       "Column class",
                "description": "The class to wrap each column",
                "value":       "col-md-%d"
            }
        }


    }
}